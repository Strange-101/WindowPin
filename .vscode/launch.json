{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch WindowPin",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}\\main.exe",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal"
        }
    ]
}
